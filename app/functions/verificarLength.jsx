

export async function verificarLength(texto, valor, data){
    if (texto.length === 0 || valor.length === 0 || data.length === 0) {
        alert('Por favor, preencha todos os campos!');
        return;
      }
}